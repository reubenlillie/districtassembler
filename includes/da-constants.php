<?php
/**
 * Defines the constants to be used throughout DistrictAssembler
 *
 * @link https://github.com/reubenlillie/districtassembler/
 *
 * @package DistrictAssembler
 * @subpackage DistrictAssembler\includes
 * @since 0.1.0
 */

// Exit if accessed directly.
if ( ! defined( 'ABSPATH' ) ) {
    exit;
}

/**
 * Defines constants to be used throughout DistrictAssembler.
 *
 * @since 0.1.0
 */
function da_define_initial_constants() {

	/**
	 * Defines the current version of DistrictAssembler.
	 *
	 * The value should be updated to match
	 * the version number in the main plugin file header.
	 *
	 * @since 0.1.0
	 */
	define( 'DISTRICTASSEMBLER_VERSION', '0.1.0' );

} // da_define_initial_constants()
